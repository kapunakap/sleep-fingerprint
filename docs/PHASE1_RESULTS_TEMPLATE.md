# Phase-1 results

Do not fill this file with synthetic metrics. Replace it only after a verified public-data run.

Required evidence:

- exact archive size and MD5 verified;
- real-data audit counts and QC summary;
- representative raw/resampled/filtered plot;
- night-disjoint train/validation/test split;
- train-night enrollment vs held-out-night retrieval metrics;
- encoder smoke test with no same-night positive pairs;
- test/lint/typecheck results;
- independent-dataset transfer result before closing issue #1.
